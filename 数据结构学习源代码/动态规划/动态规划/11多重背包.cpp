#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
int main11()
{
	/*给有一个能承重 V 的背包，和n种物品，
    每种物品的数量有限多，我们用重量、价值和数量的三元组来表示一个物品，第 i 件物品表示为（Vi，Wi，Si），
	问在背包不超重的情况下，得到物品的最大价值是多少？*/
	int V, N;
	cin >> V >> N;
	//方法1：同一物品，分为相同的多个

	int dp[] = { 0 };
	return 0;
}
